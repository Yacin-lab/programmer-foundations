Subject
---	
	🧮 — ft_sortstr
	-----------------------------------------
	Directory : ft_sortstr/
	Files : flowchart, ft_sortstr.c 
	Allowed functions : malloc (optional)
	-----------------------------------------
	▪️Subject : 
	Write a function that takes a string and reorders its content so that:
	- All letters come first.
	- All  digits come after.
	- The original order  of letters and digits within their groups must be preserved.
	▪️Example :
	char str[] = "a1b2c3";
	ft_sortstr(str);
	printf("%s\n", str);  //  print : "abc123"
	-----------------------------------------